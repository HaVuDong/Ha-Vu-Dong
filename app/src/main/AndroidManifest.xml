<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.havudong.havudong">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:label="MyLoginApp"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">

        <activity android:name=".MainActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotpassActivity" />
        <activity android:name=".User.UserInfoActivity" />
        <activity android:name=".User.ChangePassActivity" />
        <activity android:name=".ProductDetailActivity"/>
        <activity android:name=".CartActivity"/>
        <activity android:name=".CategoryProductsActivity"/>
        <activity android:name=".PaymentActivity"/>
        <!-- 👇 Đây là activity chính, cần thêm android:exported -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
